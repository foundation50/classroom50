{
  "1.3.1": {
    "status": "partially",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) + axe accessibility-tree inspection: landmarks are present (main, nav 'Primary'), form controls are labeled, and the sidebar list markup is now valid (<ul> > <li> > <a>; axe list/listitem clean). Still Partially: the heading hierarchy is inconsistent — an H3 ('Update workflow files...') appears before the page H1, and each org card exposes both an H2 and an H3 with the same text — so the programmatic structure does not yet cleanly match the visual structure."
  },
  "1.4.13": {
    "status": "partially",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) inspection of the tooltips on /accessibility: they are DaisyUI CSS .tooltip on non-focusable <span> elements, so they appear on mouse hover only (not keyboard focus), have no Escape-to-dismiss, and are not hoverable (CSS pseudo-content). They elaborate on already-visible values, so essential info is not lost, but the hover/focus content itself is not dismissable/hoverable/keyboard-reachable per 1.4.13."
  },
  "2.1.1": {
    "status": "partially",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard-only pass after the a11y fixes: the Organizations, classrooms, assignment-table, and Members/Roster flows are operable without a mouse — every control is reachable by Tab and actuatable, assignment/member/roster rows now open via a real focusable name button (Enter opens the detail modal, verified), and modals trap focus + close on Escape. Marked Partially because a keyboard-only sweep of every remaining flow (submissions dashboard, roster import, student accept/submit, org setup) and a speaking-screen-reader pass are not yet done."
  },
  "2.1.2": {
    "status": "supports",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard test of the About modal (native <dialog>): on open, focus moves into the dialog; Tab cycles only among the dialog's controls (focus stays contained across 6 presses); Escape closes it and focus returns to the triggering button. No keyboard trap; escape is always available."
  },
  "2.4.1": {
    "status": "partially",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard test: a 'Skip to main content' link is the first focusable element and has a visible focus outline, and its target main#main-content precedes the nav in DOM order. However, activating it did not move keyboard focus into main (focus fell to <body>); main lacks tabindex=\"-1\", so the skip does not reliably relocate keyboard focus past the repeated blocks."
  },
  "2.4.3": {
    "status": "supports",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard pass on the Organizations flow: Tab order follows the visual reading order (skip link -> search -> sort -> view toggles -> new-org -> per-card actions/links), with no illogical jumps. Verified on the primary authenticated flow."
  },
  "2.4.4": {
    "status": "supports",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard + accessibility-tree pass on the authenticated Organizations flow: all 33 visible links carry meaningful accessible names (e.g. 'Open', 'Review the service token for <org>'); no bare 'click here'/'link'/empty-name links found. Verified via the a11y tree, not a speaking screen reader."
  },
  "2.4.7": {
    "status": "supports",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) keyboard pass + screenshots on the Organizations flow: tabbing through search, sort, view toggles, action buttons, card links, and open dropdown-menu items shows a visible focus indicator on each (2px solid outline, plus a background change on menu items). Native inputs/selects confirmed visually focus-ringed. Verified on the primary authenticated flow; a full per-route sweep remains."
  },
  "2.5.3": {
    "status": "partially",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) accessibility-tree inspection: several controls' accessible names do not contain their visible label text — e.g. links whose visible chip reads 'No service token' / 'Expiry not tracked' are named 'Review the service token for <org>', and the account button shows the user's name visually but is named 'Account menu'. This can break voice-control ('click <visible text>')."
  },
  "4.1.2": {
    "status": "supports",
    "evidence": "manual",
    "remark": "2026-08-05 — agent-browser (Chromium) accessibility-tree inspection on the Organizations flow: every button/link exposes an accessible name (0 nameless interactive controls); comboboxes expose expanded state and options expose selected; the modal exposes its open state and manages focus. Minor exception: the card action dropdown uses role=menu with <button> children (not menuitem) and ArrowDown did not rove focus among items. Verified via the a11y tree, not a speaking screen reader."
  }
}
