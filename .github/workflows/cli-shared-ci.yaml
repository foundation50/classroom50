name: cli-shared CI

on:
  push:
    branches: [main]
    paths:
      - 'cli/shared/**'
      - '.github/workflows/cli-shared-ci.yaml'
  pull_request:
    paths:
      - 'cli/shared/**'
      - '.github/workflows/cli-shared-ci.yaml'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: cli/shared
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: cli/shared/go.mod
          cache-dependency-path: cli/shared/go.sum

      - name: Verify go.mod / go.sum are tidy
        run: |
          go mod tidy
          git diff --exit-code go.mod go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@ba0d7d2ec06a0ea1cb5fa41b2e4a3ab91d21278a # v9.3.0
        with:
          working-directory: cli/shared
          version: v2.12.2

      - name: Format (gofmt + goimports)
        run: |
          diff=$(golangci-lint fmt --diff)
          if [ -n "$diff" ]; then
            echo "::error::Formatter diffs detected (run \`golangci-lint fmt\` locally to apply):"
            echo "$diff"
            exit 1
          fi

      - name: Build
        run: go build ./...

      - name: Test
        run: go test ./...
